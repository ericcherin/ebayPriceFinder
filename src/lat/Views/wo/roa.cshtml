@using lat.Models.Completed
@model SchoolImages

@using (Html.BeginForm("rawr", "wo"))
{
    <input type="submit" value="Generate report" />
    for (int i = 0; i < Model.images.Count; i++)
    {
        @Html.TextBoxFor(s => Model.images[i].url)
    }  
}