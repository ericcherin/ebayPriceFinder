<body>
    <div style="width:50%">
        <div>
            <canvas id="canvas"></canvas>
        </div>
    </div>
    <script>
        var randomScalingFactor = function() {
            return (Math.random() > 0.5 ? 1.0 : -1.0) * Math.round(Math.random() * 100);
        };
        var randomColor = function(opacity) {
            return 'rgba(' + Math.round(Math.random() * 255) + ',' + Math.round(Math.random() * 255) + ',' + Math.round(Math.random() * 255) + ',' + (opacity || '0') + ')';
        };

        var scatterChartData = {
            datasets: [
			{
			label: "",//blank
			data:[{x:0,y:0}]}
			,{
                label: "", // mean
                data: [{
                    x: 3,
                    y: 6,
                }]
            },{
                label: "not sold",
                data: [{
                    x: 1,
                    y: 6,
                }, {
                    x: 1,
                    y: 7,
                }, {
                    x: 1,
                    y: 8,
                }, {
                    x: 1,
                    y:9,
                }]
            }, {
                label: "sold",
                data: [{
                    x: 2,
                    y: 4,
                }, {
                    x: 2,
                    y: 3,
                }, {
                    x: 2,
                    y: 5,
                }, {
                    x: 2,
                    y: 6,
                }]
            },{
                label: "stats",
                data: [{
                    x: 3,
                    y: 8,
                }]
            },{
                label: "",
                data: [{
                    x: 3,
                    y: 2,
                }]
            },{
                label: "",
                data: [{
                    x: 3,
                    y: 5,
                }]
            },{
			label: "",
			data:[{x:4,y:0}]}]
        };

        $.each(scatterChartData.datasets, function(i, dataset) {
            dataset.borderColor = randomColor();
            dataset.pointBorderColor = randomColor();

            dataset.pointBackgroundColor = randomColor();

        });


		var purple = 'rgb(173,0,204)';
		var clear = 'rgb(255,255,255)';
		var green = 'rgb(0,128,0)';
		var red = 'rgb(255,0,0)';
		var orange = 'rgb(255,128,64)';

		scatterChartData.datasets[0].pointBackgroundColor = clear; //space
		scatterChartData.datasets[1].pointBackgroundColor = orange; //mean
		scatterChartData.datasets[2].pointBackgroundColor = green; //not sold
		scatterChartData.datasets[3].pointBackgroundColor = red; // sold
		scatterChartData.datasets[4].pointBackgroundColor = purple; //quartiles
		scatterChartData.datasets[5].pointBackgroundColor = purple; //quartiles
		scatterChartData.datasets[6].pointBackgroundColor = purple; //quartiles
		scatterChartData.datasets[7].pointBackgroundColor = clear;//space

		scatterChartData.datasets[0].backgroundColor = clear; //space
		scatterChartData.datasets[1].backgroundColor = clear; //mean
		scatterChartData.datasets[2].backgroundColor = red; //notsold
		scatterChartData.datasets[3].backgroundColor = green; // sold
		scatterChartData.datasets[4].backgroundColor = purple; //stats
		scatterChartData.datasets[5].backgroundColor = clear; //quartiles
		scatterChartData.datasets[6].backgroundColor = clear; //quartiles
		scatterChartData.datasets[7].backgroundColor = clear;//space

        window.onload = function() {
            var ctx = document.getElementById("canvas").getContext("2d");
            window.myScatter = Chart.Scatter(ctx, {
                data: scatterChartData,
                options: {
				//scaleShowLabels : false,
                    title: {
                        display: true,
                        text: 'Chart.js Scatter Chart'
                    },
                    scales: {
                        xAxes: [{
						display: false,
                            position: 'top',

                        }]

                    }
                }
            });

        };


    </script>
</body>
